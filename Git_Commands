----------------------------------------Git Commands----------------------------------------

--------------------Initialize----------------------
//SETT UP:(https://docs.github.com/en/github/importing-your-projects-to-github/adding-an-existing-project-to-github-using-the-command-line)
//More Info: (https://youtu.be/tRZGeaHPoaw?t=194)

//Initialize Git by creating a .git file
git init 

# Sets the new remote
$ git remote add origin  <REMOTE_URL> 

# Verifies the new remote URL
$ git remote -v

Set your username: git config --global user.name "FIRST_NAME LAST_NAME"
Set your email address: git config --global user.email "xxxxxx1@gmail.com"


--------------------Modify/Add/Change/Commit/Push/etc...----------------------
git add . //Take a picture of the .element(which is all files in the currentPath(Can be changed to any fileName))
git commit -m 'project change' 
git.status

//see files
git log

//add file
touch style.css
git rm --cached style.css //ignore le fichier style.css

// on peut aussi ajouter un fichier .gitignore et y inclure tous les fichiers qu'on veut pas commit (ignorer)
//recuperer un fichier
git checkout -- .

//créer une branche
git branch branchname

//lister les branches
git branch --list

//switch branch
git checkout branchname

//merge branches
git merge branchname

//
git remote

git push origin master / git push -u origin master

//voir gitHub
git clone link

//Then npm i to install all packages from packages.json


// remove git
rm -rf .git

-------------------Working With team------------------------
git remote add "url" //for each member of the team

git pull origin master //for each member of the team

//Give writing right in github